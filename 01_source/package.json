{
  "name": "kptc-scheduler-web",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite --config vite.dev.config.ts",
    "build": "pnpm run build:origin && pnpm run build:tamanegi && pnpm run build:renkon && node scripts/write-release-manifest.mjs",
    "build:origin": "vite build --config vite.origin.config.ts && node scripts/copy-distribution-files.mjs origin",
    "build:tamanegi": "vite build --config vite.tamanegi.config.ts && node scripts/copy-distribution-files.mjs tamanegi",
    "build:renkon": "node scripts/copy-distribution-files.mjs renkon",
    "test": "node --test tests/rendered-html.test.mjs",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.2",
    "typescript": "5.9.3",
    "vite": "8.0.13"
  },
  "type": "module"
}
